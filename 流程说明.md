# 项目流程说明(给学习者的导览)—— 分割测量版

## 一句话架构

**平板 = 触摸屏遥控器,电脑 = 大脑。** 所有 YOLO 分割推理、训练和几何测量都在电脑上,平板浏览器只负责发送请求和显示结果。两边靠同一 WiFi 局域网连接,不需要互联网。

## 文件地图

```
yolo-web/
├── app.py                服务端全部逻辑(唯一 Python 文件)
│                         ├─ 分割推理 + 几何测量(measure_objects)
│                         ├─ 标注存取(矩形/多边形,自动回显)
│                         └─ 后台训练线程(yolo11n-seg 迁移学习)
├── templates/
│   ├── index.html        首页:图片检测+测量表、摄像头实时、知识讲解
│   ├── annotate.html     标注页:上传图片、矩形/多边形标注、回显已存标注
│   └── train.html        训练页:启动训练、看进度和结果
├── dataset/
│   ├── images/           你上传的原始图片
│   ├── labels/           每张图的标注(同名 .txt,一行一个多边形)
│   ├── classes.json      类别名单(顺序即编号)
│   ├── data.yaml         训练时自动生成,告诉 ultralytics 数据在哪
│   ├── train/            训练时自动划分:80% 训练集
│   └── val/              训练时自动划分:20% 验证集
└── models/
    ├── custom.pt         训练完成后的自定义分割模型(首页自动启用)
    └── run/              ultralytics 的训练输出(含曲线图 results.png)
```

## 完整使用流程(工业缺陷检测场景)

```
① 电脑启动服务          python app.py  →  监听 0.0.0.0:8000
② 平板打开首页          http://192.168.2.57:8000(以 ipconfig 实际 IP 为准)
③ 上传图片(标注页)     POST /api/upload       → 存入 dataset/images/
④ 设置类别(标注页)     POST /api/classes      → 存入 classes.json
                        (如: hole,pit,weld,outer_ring)
⑤ 多边形描边(标注页)  POST /api/label        → 存成分割格式 labels/*.txt
   沿缺陷/工件边缘逐点点击,≥3 点后「闭合多边形」;
   已保存的标注点开图片会自动回显(GET /api/label/<图片名>)
⑥ 启动训练(训练页)     POST /api/train/start  → 后台线程跑 yolo11n-seg
⑦ 轮询进度(训练页)     GET /api/train/status  → 进度条、日志、mAP
⑧ 训练完成              best.pt → custom.pt,首页模型自动切换
⑨ 首页测量              POST /detect(带 mm_per_px 标定系数)
                        → 检测分类 + 面积/外径/内径/焊缝宽度表
```

## 一次「检测+测量」请求的完整旅程

1. 平板 JS 把图片和标定系数(1 像素 = 多少毫米)放进 `FormData`,`POST /detect`
2. 后端解码图片 → 长边压到 960px(标定系数等比折算)→ `yolo11n-seg` 前向
3. 得到每个目标的:类别、置信度、检测框、**像素级掩码**
4. `measure_objects()` 对每个掩码做几何分析:
   - 掩码放大回原图分辨率 → `findContours` 提取轮廓层级
   - 外轮廓 → **面积**(像素数 × 标定系数²)、**外径**(最小外接圆直径)
   - 内部孔洞 → **内径**(最大孔洞等效圆直径)
   - 细长形状(长宽比 ≥ 3)→ **焊缝宽度**(最小外接矩形短边)
5. 结果打包 JSON 返回:base64 标注图 + 测量表,平板直接渲染

## 一次训练在电脑上发生了什么

1. 收集所有**已标注**的图片,随机 8:2 分成训练/验证集,生成 `data.yaml`
2. 加载预训练 `yolo11n-seg.pt` 做**迁移学习**(COCO 预训练 + 你的缺陷类别)
3. 每轮结束触发回调 → 更新全局 `train_state` → 平板轮询可见
   (分割模型看 `mAP50(M)` 掩码指标,比框指标更能反映掩码质量)
4. 训练结束取 `best.pt` 存为 `custom.pt`,热替换首页模型

## YOLO 分割标注格式(labels/*.txt)

每行一个目标:`类别编号 x1 y1 x2 y2 x3 y3 ...`(沿边缘的顶点序列):

```
0 0.10 0.10 0.50 0.10 0.50 0.60 0.20 0.70
└ 类别0 └ 4 个顶点的归一化坐标(0~1)
```

- 坐标是 0~1 比例值,同一标注适用于任何分辨率
- 浏览器里矩形标注会被转成四角多边形保存
- 旧版"框格式"(5 个数)启动时自动迁移,但**多边形描边精度更高**,
  想要准确的内外径/焊缝宽,务必用多边形模式重标关键图片

## 关键 API 一览

| 接口 | 方法 | 作用 |
|---|---|---|
| `/detect` | POST | 上传图片 + mm_per_px,返回检测结果与测量表 |
| `/video` | GET | 摄像头实时分割流(MJPEG) |
| `/api/upload` | POST | 批量上传待标注图片 |
| `/api/classes` | GET/POST | 读/写类别名单 |
| `/api/label` | POST | 保存一张图的标注(矩形或多边形,统一存多边形) |
| `/api/label/<图片名>` | GET | 读取已保存标注,标注页回显用 |
| `/api/train/start` | POST | 启动后台训练线程 |
| `/api/train/status` | GET | 数据统计 + 训练进度(轮询用) |
| `/api/train/curve` | GET | 训练曲线图 results.png |

## 测量精度要点(做尺寸测量前必读)

1. **标定**:拍摄平面放一把尺,量出 N mm 对应多少像素,N÷像素数 填进首页输入框
2. **拍摄姿态**:正对被测面俯拍,固定距离和焦段,避免透视畸变
3. **掩码边缘误差**:约 1~2 像素,对 mm 级精密测量先用已知尺寸工件验证误差
4. **细长判断**:焊缝宽度只在目标长宽比 ≥ 3 时给出;圆形工件内外径需要
   掩码能围出闭合外轮廓和内部孔洞(俯拍效果最好)

## 学习路线建议

1. **读懂推理**:看 `app.py` 的 `draw_detections()` 和 `measure_objects()` —— 掩码怎么来、几何量怎么算
2. **理解标注**:打开 `dataset/labels/` 里任何一个 .txt,对照图片看多边形顶点
3. **理解训练**:把 `run_training()` 里的参数改一改(epochs、imgsz),观察训练时间和 mAP 变化
4. **进阶方向**:换 yolo11s-seg 更准;给测量加周长/圆度;研究 `models/run/results.png` 曲线

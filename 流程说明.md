# 项目流程说明(给学习者的导览)

## 一句话架构

**平板 = 触摸屏遥控器,电脑 = 大脑。** 所有 YOLO 推理和训练都在电脑上,平板浏览器只是发送请求和显示结果。两边靠同一 WiFi 局域网连接,不需要互联网。

## 文件地图

```
yolo-web/
├── app.py                服务端全部逻辑(唯一 Python 文件,约 400 行)
├── templates/
│   ├── index.html        首页:图片检测 + 摄像头实时 + 知识讲解
│   ├── annotate.html     标注页:上传图片、画框、保存标注
│   └── train.html        训练页:启动训练、看进度和结果
├── dataset/
│   ├── images/           你上传的原始图片
│   ├── labels/           每张图的标注(同名 .txt,一行一个框)
│   ├── classes.json      类别名单(顺序即编号)
│   ├── data.yaml         训练时自动生成,告诉 ultralytics 数据在哪
│   ├── train/            训练时自动划分:80% 训练集
│   └── val/              训练时自动划分:20% 验证集
└── models/
    ├── custom.pt         训练完成后的自定义模型(首页自动启用)
    └── run/              ultralytics 的训练输出(含曲线图 results.png)
```

## 完整使用流程

```
① 电脑启动服务          python app.py  →  监听 0.0.0.0:8000
② 平板打开首页          http://192.168.2.57:8000(以 ipconfig 实际 IP 为准)
③ 上传图片(标注页)     POST /api/upload   → 存入 dataset/images/
④ 设置类别(标注页)     POST /api/classes  → 存入 classes.json
⑤ 画框保存(标注页)     POST /api/label    → 转成 YOLO 格式存 labels/
⑥ 启动训练(训练页)     POST /api/train/start → 后台线程开始训练
⑦ 轮询进度(训练页)     GET /api/train/status → 进度条、日志、mAP
⑧ 训练完成              best.pt → custom.pt,首页检测模型自动切换
⑨ 回到首页测试           POST /detect → 用你的自定义模型识别
```

## 一次图片检测请求的完整旅程(以首页上传为例)

1. 平板 JS 把文件放进 `FormData`,`fetch POST /detect`
2. 后端读出字节 → OpenCV 解码为像素矩阵 → 长边压缩到 960px(CPU 提速)
3. `model.predict()` 前向推理,得到每个目标的:类别、置信度、边界框坐标
4. 置信度 < 0.4 的结果被过滤;其余画框、编码 JPEG、转 base64
5. 打包成 JSON 返回;平板 JS 把 base64 塞进 `<img src>` 直接显示

## 一次训练在电脑上发生了什么

1. 收集所有**已标注**的图片(有对应 .txt 才算),随机 8:2 分成训练/验证集
2. 生成 `data.yaml`(数据路径 + 类别表)——ultralytics 的训练入口配置
3. 加载预训练 `yolo11n.pt` 做**迁移学习**:不是从零认识物体,而是在"已认识 80 类"的基础上学认你的新类别,所以几十张图就够
4. 每轮(epoch)结束触发回调 → 更新全局 `train_state` → 平板轮询可见
5. 训练结束取 `best.pt`(验证集上表现最好的那轮)存为 `custom.pt`,并热替换首页的检测模型

## YOLO 标注格式(labels/*.txt)

每行一个框:`类别编号 x中心 y中心 宽 高`,坐标全是 **0~1 的比例值**(除以图片宽/高),所以同一标注适用于任何分辨率:

```
0 0.250000 0.400000 0.300000 0.400000
└ 类别0   └ 中心在宽25%、高40% 处,框宽30%、高40%
```

浏览器里画框是"左上角+宽高",后端 `x_center = x + w/2` 转换。

## 关键 API 一览

| 接口 | 方法 | 作用 |
|---|---|---|
| `/detect` | POST | 上传图片,返回检测结果 |
| `/video` | GET | 摄像头实时检测流(MJPEG) |
| `/api/upload` | POST | 批量上传待标注图片 |
| `/api/classes` | GET/POST | 读/写类别名单 |
| `/api/label` | POST | 保存一张图的标注框 |
| `/api/train/start` | POST | 启动后台训练线程 |
| `/api/train/status` | GET | 数据统计 + 训练进度(轮询用) |
| `/api/train/curve` | GET | 训练曲线图 results.png |

## 学习路线建议

1. **读懂推理**:看 `app.py` 的 `draw_detections()` 和 `/detect` 接口 —— 30 行就能理解 YOLO 怎么用
2. **理解标注**:打开 `dataset/labels/` 里任何一个 .txt,对照图片看坐标含义
3. **理解训练**:把 `run_training()` 里的参数改一改(epochs、imgsz、conf),观察训练时间和 mAP 变化
4. **进阶方向**:换成 yolo11s.pt 更准;给 `/video` 加 FPS 显示;研究 `models/run/results.png` 里各条曲线的含义
